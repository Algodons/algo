# Backend Development Environment Configuration

# Server Configuration
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=algo_ide_dev
DB_USER=algo_user
DB_PASSWORD=dev_password

# JWT Configuration
JWT_SECRET=dev-secret-key-at-least-32-characters-long-for-testing
JWT_EXPIRATION=7d

# Encryption Configuration
ENCRYPTION_SECRET=dev-encryption-key-at-least-32-characters-for-testing

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Copilot SaaS API Configuration
COPILOT_API_URL=https://api-dev.copilot.example.com
COPILOT_API_KEY=dev_test_key_replace_with_actual
COPILOT_ENABLED=true

# AI/ML Service Configuration
AI_SERVICE_ENABLED=true
ML_MODELS_ENABLED=true

# Team Billing Pricing (Dev/Test values)
COMPUTE_COST_PER_HOUR=0.01
STORAGE_COST_PER_GB=0.001
BANDWIDTH_COST_PER_GB=0.002
BILLING_CURRENCY=USD

# Rate Limiting (Relaxed for dev)
RATE_LIMIT_WINDOW=15m
RATE_LIMIT_MAX_REQUESTS=1000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# Logging
LOG_LEVEL=debug
LOG_FORMAT=dev
DEBUG=true
VERBOSE_LOGGING=true

# Development Flags
HOT_RELOAD=true
AUTO_RESTART=true
ENABLE_DEBUG_ROUTES=true

# Stripe (Test mode)
STRIPE_SECRET_KEY=sk_test_your_stripe_test_key
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret

# Email Configuration (MailCatcher or similar)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=dev@algo-ide.local

# Feature Flags
ENABLE_COLLABORATION=true
ENABLE_GIT_INTEGRATION=true
ENABLE_MONITORING=true
